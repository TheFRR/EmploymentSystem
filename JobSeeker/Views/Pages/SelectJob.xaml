<Page x:Class="JobSeeker.Views.Pages.SelectJob"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewmodels="clr-namespace:JobSeeker.ViewModels"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:JobSeeker.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SelectJob" Style="{StaticResource Page}">

    <Page.DataContext>
        <viewmodels:VMSelectJob/>
    </Page.DataContext>

    <Grid Width="180">
        <StackPanel VerticalAlignment="Center">
            <TextBlock FontSize="16" TextAlignment="Center">Должность</TextBlock>
            <ComboBox Style="{StaticResource MaterialDesignComboBox}" Margin="0,10,0,0"
                      IsEditable="True" FontSize="16">
                <TextBlock>Лягушка</TextBlock>
                <TextBlock>Жаба</TextBlock>
                <TextBlock>Мемная легушька</TextBlock>
            </ComboBox>
            <Button Margin="0,10,0,0" Command="{Binding SelectTestCommand}">Начать тест</Button>
        </StackPanel>
    </Grid>
</Page>
