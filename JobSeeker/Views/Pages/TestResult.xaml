<Page x:Class="JobSeeker.Views.Pages.TestResult"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:JobSeeker.Views.Pages"
      xmlns:viewmodels="clr-namespace:JobSeeker.ViewModels"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TestResult" Style="{StaticResource Page}">

    <Page.DataContext>
        <viewmodels:VMTestResult />
    </Page.DataContext>

    <Grid Width="450">
        <StackPanel VerticalAlignment="Center">
            <TextBlock FontSize="24" TextAlignment="Center" Margin="5,0,5,10">
                <Run>К сожалению,</Run>
                <Run Foreground="Red">Вы не лягушка...</Run>
            </TextBlock>
            <Separator/>
            <TextBlock FontSize="16" Margin="5,5,0,0">Правильных ответов: 0/20</TextBlock>
            <TextBlock FontSize="16" Margin="5,5,0,0">Времени затрачено: 0:25:36</TextBlock>
            <Button Margin="5,10,5,0" Width="250" Command="{Binding BackToTestsCommand}">Вернуться к выбору тестов</Button>
        </StackPanel>
    </Grid>
</Page>
